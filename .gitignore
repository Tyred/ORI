#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main()
{
    char nome[100], auxnome[100];
    int i = 0, j=0, tam;

    gets(nome);

    tam = strlen(nome);

    while(nome[i]!= '\0')
    {
        if(nome[i] != ' ')
        {
           auxnome[j] = nome[i];
           j++;
        }

        i++;

    }
        printf("%s", auxnome);

}

